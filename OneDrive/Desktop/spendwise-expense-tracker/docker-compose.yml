version: '3'

services:

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://sagarbulti:SagarBB2005@cluster0.cuesdjn.mongodb.net/?appName=Cluster0
      - JWT_SECRET=secretkey

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend